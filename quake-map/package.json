{
  "name": "@quake/map",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "pages:dev": "wrangler pages dev dist --compatibility-date=2024-01-01 --do QUAKE_SERVICE=quake-service",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest",
    "deploy": "npm run build && wrangler pages deploy dist"
  },
  "dependencies": {
    "@quake/shared": "*"
  },
  "devDependencies": {}
}
